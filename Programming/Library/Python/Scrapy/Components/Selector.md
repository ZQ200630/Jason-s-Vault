---
tags:  自动化 Python Scrapy
alias: 
---
# Selector

```python
response.xpath('')  --bytes 
		[[xpath]]过滤
    xpath().extract()  
		#返回结果集
        extract()[0]  
				#返回第一个元素

response.css('') --bytes  
[[css]]选择器,同上

response.re('') 
#其结果不需要格式化
```